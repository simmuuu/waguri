[project]
name = "waguri"
version = "0.1.0"
description = "a discord bot i made cause i always wanted to"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "aiosqlite>=0.22.1",
    "discord-py>=2.7.1",
    "mcstatus>=13.1.0",
    "python-dotenv>=1.2.2",
]
